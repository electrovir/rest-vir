## Rewrite

-   make sure all shape validation uses "allowExtraKeys: true"
-   fix all `as never` instances
-   manually run "does not get blocked" test
-   implement the demo
-   implement the mock host
