import {defineElement, html} from 'element-vir';

export const VirDemo = defineElement()({
    tagName: 'vir-demo',
    render() {
        return html`
            Rest Vir Demo
        `;
    },
});
